const db = require('./db');
const bcrypt = require('bcrypt'); // if bcrypt is not installed, I might just use plain text for now, but wait, does backend have bcrypt? let me check package.json.
// Wait, package.json didn't have bcrypt!
// I'll just use plaintext or I'll install bcrypt?
// I will just use plain text or a simple hardcode in the DB for now, or maybe bcrypt?
// Actually, let me use a simple plaintext for this prototype, or I can run npm install bcrypt.
